/**
 * @author single
 * @date 2018/4/12.
 */
public class HeapSort<T> {
    public T[] sort(T[] input) {
        if (input.length <= 1) {
            return input;
        }
        int middle = input.length >>> 2;
        for (int i = middle; i >= 0; i--) {

        }
        return input;
    }
}
